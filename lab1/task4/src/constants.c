#include <math.h>

#include "approximate_values/constants.h"

const long double EXP = exp(1);
const long double PI = acos(-1);
const long double LN_2 = log(2);
const long double SQRT_2 = sqrt(2);
const long double EULER_MASCHERONI = 0.5772156649;
